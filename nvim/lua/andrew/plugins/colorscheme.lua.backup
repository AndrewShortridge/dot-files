return {
  "olimorris/onedarkpro.nvim",
  priority = 1000,
  config = function()
    require("onedarkpro").setup({
      options = {
        transparency = false,
        terminal_colors = true,
      },
      styles = {
        comments = "italic",
        keywords = "bold",
      },
    })

    -- Use the darker theme variant
    vim.cmd("colorscheme onedark_dark")
  end,
}
