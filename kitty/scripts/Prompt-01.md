spawn as many subagents as necessary to implement ALL the issues in git worktrees. phase 1 should be issue 1, then once that is complete merge, phase 2 should be ALL issues dependent on issue 1, then merge, then phase 3 should be ALL the issues dependen on issue 2, then merge, then phase 4 should be the remaining issues. spawn an implementation subagent for ALL individual implementation AND merging/resolving any merge conflicts
